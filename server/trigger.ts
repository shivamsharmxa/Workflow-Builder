// Trigger.dev v4 uses a different API
// For now, we'll create a placeholder that we'll implement properly later
// V4 uses background tasks instead of the old client approach

export const client = {
  // Placeholder for v4 migration
  defineJob: () => {
    console.warn('Trigger.dev jobs need to be migrated to v4 API');
  }
};
